# comparison1-# Мануал: автоматизированная загрузка цен в 1С (УТ) через Excel и comparison1c

Этот процесс нужен для автоматизированной загрузки цен в 1С из любых внешних источников (например, **Memo Fora**).  
Суть: мы выгружаем шаблон прайс-листа из 1С, подставляем в него актуальные цены через приложение **comparison1c**, а затем загружаем обратно в 1С.

---

## Что потребуется

- Доступ к **1С: Управление торговлей (УТ)**
- Excel-файл с актуальными ценами из внешнего сервиса (например, Memo Fora)
- Приложение **comparison1c** (EXE)
- Microsoft Excel (или любой редактор .xlsx)

---

## Общая логика процесса

1. В 1С формируется и выгружается **прайс-лист** в Excel.
2. Во внешней таблице/источнике берутся **актуальные цены**.
3. Через **comparison1c** выполняется сопоставление номенклатуры и цен.
4. Полученные корректные цены вставляются в выгруженный из 1С прайс-лист.
5. Прайс-лист загружается обратно в 1С УТ.

---

## Пошаговая инструкция

### 1) Открыть прайс-лист в 1С
1. Откройте **1С: Управление торговлей**.
2. Перейдите в подсистему **CRM и маркетинг**.
3. Найдите раздел **Цены (прайс-лист)**.

---

### 2) Сформировать и выгрузить прайс-лист в Excel
1. Сформируйте прайс-лист.
2. Нажмите кнопку **Excel** (выгрузка).
3. Укажите путь, куда сохранить файл (например: `C:\Prices\template_1c.xlsx`).

---

### 3) Подготовить таблицу для сопоставления
1. Откройте:
   - таблицу с актуальными ценами из источника (например, Memo Fora),
   - и выгруженную из 1С таблицу прайс-листа.
2. Заполните таблицу `template` (или любую аналогичную), чтобы в ней были:
   - номенклатура,
   - цены (складская/оптовая/розничная — по необходимости),
   - эталонная номенклатура из 1С (для корректного порядка и соответствия).

> Важно: цель — подготовить данные так, чтобы цены корректно соотнеслись с номенклатурой 1С.

---

### 4) Сопоставить номенклатуру и цены через comparison1c
1. Запустите приложение **comparison1c**.
2. Выберите подготовленный Excel-файл (`template`).
3. Назначьте столбцы по смыслу:
   - **Номенклатура (источник цен)**
   - **Цена складская / оптовая / розничная**
   - **Эталонная номенклатура**
   - **Единица измерения (эталон)**
4. Нажмите **Запустить сопоставление**.

📌 Приложение автоматически создаст новый Excel-файл с отдельным листом, где цены будут стоять **соответственно номенклатуре из 1С** (эталонной).

---

### 5) Вставить цены в выгруженный прайс-лист 1С
1. Откройте результат, созданный приложением **comparison1c**.
2. На листе с итогом скопируйте столбцы цен.
3. Вставьте цены в исходную таблицу, выгруженную из 1С (в нужные колонки цен).

> Важно: вставляйте **только значения**, без формул и форматирования, чтобы избежать ошибок импорта.

---

### 6) Загрузить таблицу обратно в 1С УТ
1. Вернитесь в **1С: УТ → Цены (прайс-лист)**.
2. Выберите загрузку из Excel (импорт).
3. Укажите файл прайс-листа, куда вы вставили цены.
4. Подтвердите загрузку.

---

## Примечания и советы

- Если по какой-то номенклатуре совпадение не найдено, приложение ставит **0** — такие позиции нужно проверять вручную.
- Чтобы сопоставление работало корректнее, рекомендуется:
  - использовать выгрузку из 1С как **эталон**,
  - следить за единым стилем названий (лишние пробелы, спецсимволы могут влиять).

---

## Итог

После выполнения шагов цены из внешнего источника будут корректно перенесены в прайс-лист 1С и загружены в **1С: Управление торговлей**.
